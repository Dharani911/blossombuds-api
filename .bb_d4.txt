-                                <span>Confirm new password</span>
-                                <div className="field">
-                                  <IconLock />
-                                  <input
-                                    className="input"
-                                    type="password"
-                                    placeholder="Re-enter new password"
-                                    value={fpConfirm}
-                                    onChange={(e) => setFpConfirm(e.target.value)}
-                                    required
-                                  />
-                                </div>
-                              </label>
-
-                              {fpMsg && <div className="resend-msg">{fpMsg}</div>}
-
-                              <button
-                                className="cta"
-                                type="submit"
-                                disabled={
-                                  fpBusy ||
-                                  fpCode.length < 6 ||
-                                  fpNewPw.length < 8 ||
-                                  fpNewPw !== fpConfirm
-                                }
-                              >
-                                {fpBusy ? "Updating…" : "Confirm & Login"}
-                              </button>
-
-                              <button
-                                type="button"
-                                className="back-btn"
-                                onClick={() => setStep("forgot-password")}
-                              >
-                                ← Back
-                              </button>
-                            </form>
-                          </>
-                        )}
 
           </div>
         </div>
@@ -467,9 +477,11 @@ export default function LoginModal() {
   return createPortal(modal, document.body);
 }
 
+// ── Icons ──────────────────────────────────────────────────────────────────
+
 function IconMail() {
   return (
-    <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden>
+    <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden style={{ flexShrink: 0 }}>
       <path d="M4 6l8 5 8-5" fill="none" stroke="rgba(0,0,0,.5)" strokeWidth="1.6" />
       <rect x="3" y="5" width="18" height="14" rx="2" fill="none" stroke="rgba(0,0,0,.5)" strokeWidth="1.6" />
     </svg>
@@ -478,13 +490,22 @@ function IconMail() {
 
 function IconLock() {
   return (
-    <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden>
+    <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden style={{ flexShrink: 0 }}>
       <rect x="5" y="10" width="14" height="10" rx="2" fill="none" stroke="rgba(0,0,0,.5)" strokeWidth="1.6" />
       <path d="M8 10V7a4 4 0 118 0v3" fill="none" stroke="rgba(0,0,0,.5)" strokeWidth="1.6" />
     </svg>
   );
 }
 
+function IconPhone() {
+  return (
+    <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden style={{ flexShrink: 0 }}>
+      <rect x="7" y="2" width="10" height="20" rx="2" fill="none" stroke="currentColor" strokeWidth="1.6" />
+      <circle cx="12" cy="18" r="1" fill="currentColor" />
+    </svg>
+  );
+}
+
 function EyeIcon({ open }: { open: boolean }) {
   return open ? (
     <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden>
@@ -499,6 +520,9 @@ function EyeIcon({ open }: { open: boolean }) {
     </svg>
   );
 }
+
+// ── Styles ─────────────────────────────────────────────────────────────────
+
 const styles = `
 :root{
   --bb-primary: #4A4F41;
@@ -506,414 +530,183 @@ const styles = `
   --bb-accent-2: #F6C320;
   --bb-bg: #FAF7E7;
 }
-
-/* Overlay */
 .auth-scrim{
   position:fixed; inset:0; z-index:9998;
-  background: rgba(0,0,0,.28);
-  backdrop-filter: blur(6px);
-  animation: fadeIn .18s ease both;
+  background:rgba(0,0,0,.28); backdrop-filter:blur(6px);
+  animation:fadeIn .18s ease both;
 }
-@keyframes fadeIn{ from{ opacity:0 } to{ opacity:1 } }
-
-/* Centered container */
+@keyframes fadeIn{ from{opacity:0} to{opacity:1} }
 .auth-modal{
   position:fixed; inset:0; z-index:9999;
-  display:grid; place-items:center;
-  padding:16px;
+  display:grid; place-items:center; padding:16px;
 }
-
-/* Sheet card */
 .sheet{
-  display: flex;
-  flex-direction: column;
-  width: min(440px, 96vw);
-  max-height: min(88vh, 680px);
-  background: var(--bb-bg);
-  border-radius: 22px;
-  box-shadow: 0 24px 80px rgba(0,0,0,.22);
-  animation: popUp .22s cubic-bezier(.2,.8,.2,1) both;
-  overflow: hidden;
+  display:flex; flex-direction:column;
+  width:min(440px,96vw); max-height:min(88vh,700px);
+  background:var(--bb-bg); border-radius:22px;
+  box-shadow:0 24px 80px rgba(0,0,0,.22);
+  animation:popUp .22s cubic-bezier(.2,.8,.2,1) both;
+  overflow:hidden;
 }
 @keyframes popUp{
-  from{ opacity:0; transform: scale(.96) translateY(12px); }
-  to{ opacity:1; transform:none; }
+  from{opacity:0;transform:scale(.96) translateY(12px)}
+  to{opacity:1;transform:none}
 }
-
-/* Header – shared for login + register */
 .am-head{
-  display:flex; align-items:center; justify-content:space-between; gap: 10px;
-  padding: 12px 18px;
-  border-bottom: 1px solid rgba(0,0,0,.06);
-  flex-shrink: 0;
-  background: linear-gradient(
-    135deg,
-    rgba(240,93,139,.22),
-    rgba(246,195,32,.18)
-  );
-  box-shadow:
-    0 3px 10px rgba(0,0,0,.06),
-    0 1px 0 rgba(255,255,255,.55) inset;
+  display:flex; align-items:center; justify-content:space-between; gap:10px;
+  padding:12px 18px; border-bottom:1px solid rgba(0,0,0,.06); flex-shrink:0;
+  background:linear-gradient(135deg,rgba(240,93,139,.22),rgba(246,195,32,.18));
+  box-shadow:0 3px 10px rgba(0,0,0,.06),0 1px 0 rgba(255,255,255,.55) inset;
 }
-.brand{ display:flex; align-items:center; gap: 10px; }
-.brand img{ height: 40px; }
+.brand{ display:flex; align-items:center; gap:10px; }
+.brand img{ height:40px; }
 .brand-name{ display:flex; flex-direction:column; }
 .brand-name .big{
-  font-family: "DM Serif Display", Georgia, serif;
-  font-size: 16px;
-  color: #2f332a;
-}
-.brand-name .small{
-  font-size: 11px;
-  color: #4A4F41;
-  opacity:.9;
+  font-family:"DM Serif Display",Georgia,serif; font-size:16px; color:#2f332a;
 }
+.brand-name .small{ font-size:11px; color:#4A4F41; opacity:.9; }
 .x{
-  width:34px; height:34px;
-  border-radius:10px;
-  border:1px solid rgba(0,0,0,.1);
-  background:rgba(255,255,255,.9);
-  cursor:pointer;
-  display:inline-flex;
-  align-items:center;
-  justify-content:center;
+  width:34px; height:34px; border-radius:10px;
+  border:1px solid rgba(0,0,0,.1); background:rgba(255,255,255,.9);
+  cursor:pointer; display:inline-flex; align-items:center; justify-content:center;
 }
-
-/* Scrollable body – mild ivory, not pure white */
 .sheet-scroll{
-  flex: 1 1 auto;
-  min-height: 0;
-  overflow: auto;
-  overscroll-behavior: contain;
-  -webkit-overflow-scrolling: touch;
-  padding: 12px 16px 16px;
-  background:#FFFBF3;            /* 🌼 ivory instead of white */
-  border-radius: 0 0 22px 22px;
-}
-
-/* Hero */
-.hero{
-  padding: 6px 2px 10px;
-  text-align:left;
+  flex:1 1 auto; min-height:0; overflow:auto; overscroll-behavior:contain;
+  -webkit-overflow-scrolling:touch; padding:12px 16px 16px;
+  background:#FFFBF3; border-radius:0 0 22px 22px;
 }
+.hero{ padding:6px 2px 10px; }
 .hero h3{
-  margin:0;
-  font-family: "DM Serif Display", Georgia, serif;
-  color: var(--bb-primary);
-  font-size: 24px;
-}
-.hero p{
-  margin: 6px 0 0;
-  color: var(--bb-primary);
-  opacity:.92;
-}
-
-/* Form */
-.form{
-  display:flex;
-  flex-direction:column;
-  gap:12px;
-  width:100%;
+  margin:0; font-family:"DM Serif Display",Georgia,serif;
+  color:var(--bb-primary); font-size:24px;
 }
-.label{
-  display:flex;
-  flex-direction:column;
-  gap:6px;
-  width:100%;
-}
-.label > span{
-  font-weight:800;
-  color: var(--bb-primary);
-  font-size: 13px;
-}
-
-/* Inputs */
+.hero p{ margin:6px 0 0; color:var(--bb-primary); opacity:.92; }
+.form{ display:flex; flex-direction:column; gap:12px; width:100%; }
+.label{ display:flex; flex-direction:column; gap:6px; width:100%; }
+.label > span{ font-weight:800; color:var(--bb-primary); font-size:13px; }
 .field{
   display:flex; align-items:center; gap:8px;
-  border:1px solid rgba(0,0,0,.12);
-  background:#fff;
-  border-radius: 14px;
-  padding: 0 10px;
-  transition: border-color .16s ease, box-shadow .16s ease, background .16s ease;
+  border:1px solid rgba(0,0,0,.12); background:#fff;
+  border-radius:14px; padding:0 10px;
+  transition:border-color .16s,box-shadow .16s,background .16s;
 }
 .field:focus-within{
-  border-color: rgba(246,195,32,.95);
-  box-shadow: 0 0 0 6px rgba(246,195,32,.16);
-  background: #FFFEF8;
+  border-color:rgba(246,195,32,.95);
+  box-shadow:0 0 0 6px rgba(246,195,32,.16); background:#FFFEF8;
 }
 .input{
-  height: 48px; min-height:48px;
-  border:none; outline:none;
-  flex:1;
-  color: var(--bb-primary);
-  background: transparent;
-  font-weight: 600;
-  font-size:16px;
-}
-
-/* Hints */
-.hint{
-  font-size: 12px;
-  color: var(--bb-primary);
-  opacity:.8;
-  margin-top: 4px;
-  display:block;
+  height:48px; min-height:48px; border:none; outline:none;
+  flex:1; color:var(--bb-primary); background:transparent;
+  font-weight:600; font-size:16px;
 }
-.hint.ok{
-  color:#0c6e3c;
-  opacity:1;
+.phone-prefix{
+  font-size:15px; font-weight:700; color:var(--bb-primary); opacity:.7;
+  padding-right:6px; border-right:1px solid rgba(0,0,0,.1); margin-right:2px; white-space:nowrap;
 }
-
-/* Error */
+.hint{ font-size:12px; color:var(--bb-primary); opacity:.8; margin-top:4px; display:block; }
+.hint.ok{ color:#0c6e3c; opacity:1; }
 .error{
-  background:#fff3f5;
-  border:1px solid rgba(240,93,139,.25);
-  color:#b0003a;
-  padding:8px 10px;
-  border-radius:12px;
-  font-size:13px;
+  display:flex; flex-direction:column; gap:6px;
+  background:#fff3f5; border:1px solid rgba(240,93,139,.25);
+  color:#b0003a; padding:8px 10px; border-radius:12px; font-size:13px;
 }
-
-/* CTA */
-.cta{
-  width: 100%;
-  height: 48px;
-  border-radius: 12px;
-  border: none;
-  cursor: pointer;
-  padding: 0 16px;
-  background: linear-gradient(135deg, var(--bb-accent), #ff7aa6);
-  color:#fff;
-  font-weight:900;
-  letter-spacing:.2px;
-  box-shadow: 0 10px 28px rgba(240,93,139,.35);
-  transition: transform .16s ease, box-shadow .16s ease, opacity .16s ease;
-  margin-top: 6px;
-}
-.cta:hover{
-  transform: translateY(-1px);
-  box-shadow: 0 12px 34px rgba(240,93,139,.45);
-}
-.cta[disabled]{
-  opacity:.65;
-  cursor:not-allowed;
-  transform:none;
-  box-shadow:none;
-}
-
-/* Links */
-.links{
-  display:flex;
-  justify-content:center;
-  gap: 8px;
-  align-items:center;
-  margin-top: 8px;
-}
-.muted{
-  color: var(--bb-primary);
-  opacity:.8;
-  font-size: 13px;
+.resend-btn{
+  background:transparent; border:1px solid rgba(240,93,139,.4);
+  color:#b0003a; border-radius:8px; padding:4px 10px; font-size:12px;
+  cursor:pointer; font-weight:600; align-self:flex-start;
 }
+.cta{
+  width:100%; height:48px; border-radius:12px; border:none; cursor:pointer;
+  background:linear-gradient(135deg,var(--bb-accent),#ff7aa6); color:#fff;
+  font-weight:900; letter-spacing:.2px;
+  box-shadow:0 10px 28px rgba(240,93,139,.35);
+  transition:transform .16s,box-shadow .16s,opacity .16s; margin-top:6px;
+}
+.cta:hover{ transform:translateY(-1px); box-shadow:0 12px 34px rgba(240,93,139,.45); }
+.cta[disabled]{ opacity:.65; cursor:not-allowed; transform:none; box-shadow:none; }
+.links{ display:flex; justify-content:center; gap:8px; align-items:center; margin-top:8px; }
+.single{ justify-content:center; }
+.muted{ color:var(--bb-primary); opacity:.8; font-size:13px; }
 .textlink{
-  background: transparent;
-  border: none;
-  padding: 0;
-  color: var(--bb-accent);
-  font-size: 14px;
-  font-weight: 700;
-  text-decoration: underline;
-  cursor: pointer;
-  opacity: .92;
-  transition: color .15s ease, opacity .15s ease;
-}
-.textlink:hover{
-  color: #d94676;
-  opacity:1;
-}
-.textlink[disabled]{
-  opacity:.5;
-  cursor:not-allowed;
-}
-
-/* Eye toggles */
+  background:transparent; border:none; padding:0; color:var(--bb-accent);
+  font-size:14px; font-weight:700; text-decoration:underline; cursor:pointer;
+  opacity:.92; transition:color .15s,opacity .15s;
+}
+.textlink:hover{ color:#d94676; opacity:1; }
+.textlink[disabled]{ opacity:.5; cursor:not-allowed; }
 .icon-btn.eye{
   display:inline-flex; align-items:center; justify-content:center;
-  width: 36px; height: 36px;
-  border: none;
-  background: transparent;
-  border-radius: 10px;
-  cursor: pointer;
+  width:36px; height:36px; border:none; background:transparent;
+  border-radius:10px; cursor:pointer;
 }
-.icon-btn.eye:hover{
-  background: rgba(0,0,0,.04);
-}
-
-/* Divider */
+.icon-btn.eye:hover{ background:rgba(0,0,0,.04); }
 .divider{
-  display:flex;
-  align-items:center;
-  gap:12px;
-  margin: 16px 0 12px;
-}
-.divider::before,
-.divider::after{
-  content:"";
-  flex:1;
-  height:1px;
-  background: rgba(0,0,0,.08);
-}
-.divider span{
-  font-size:13px;
-  color: var(--bb-primary);
-  opacity:.7;
-  font-weight:600;
-}
-
-/* Google button base */
+  display:flex; align-items:center; gap:12px; margin:14px 0 10px;
+}
+.divider::before,.divider::after{
+  content:""; flex:1; height:1px; background:rgba(0,0,0,.08);
+}
+.divider span{ font-size:13px; color:var(--bb-primary); opacity:.7; font-weight:600; }
 .google-btn{
   display:flex; align-items:center; justify-content:center; gap:10px;
-  width:100%; height:48px;
-  border-radius:12px;
-  background:#fff;
-  border:1px solid rgba(0,0,0,.08);
-  color: #3c4043;
-  font-weight:600;
-  font-size:15px;
-  cursor:pointer;
-  transition: background .15s ease, box-shadow .15s ease, transform .15s ease;
-}
-.google-btn img{
-  width:20px; height:20px;
+  width:100%; height:48px; border-radius:12px; background:#fff;
+  border:1px solid rgba(0,0,0,.08); color:#3c4043;
+  font-weight:600; font-size:15px; cursor:pointer;
+  transition:background .15s,box-shadow .15s,transform .15s;
 }
-
-/* Pink-theme Google button */
+.google-btn img{ width:20px; height:20px; }
 .primary-google{
-  background: linear-gradient(135deg, #F05D8B, #ff8fb0) !important;
-  border: none !important;
-  color: #fff !important;
-  font-weight: 700 !important;
-  box-shadow: 0 8px 24px rgba(240,93,139,.35);
+  background:linear-gradient(135deg,#F05D8B,#ff8fb0) !important;
+  border:none !important; color:#fff !important; font-weight:700 !important;
+  box-shadow:0 8px 24px rgba(240,93,139,.35);
 }
 .primary-google:hover{
-  background: linear-gradient(135deg, #e44a7b, #ff7aa2) !important;
-  box-shadow: 0 10px 28px rgba(240,93,139,.45);
+  background:linear-gradient(135deg,#e44a7b,#ff7aa2) !important;
+  box-shadow:0 10px 28px rgba(240,93,139,.45);
 }
-.primary-google img{
-  filter: brightness(0) invert(1);
-}
-
-/* Email toggle button */
-.email-toggle-btn{
-  display:flex; align-items:center; justify-content:center;
-  width:100%; height:48px;
-  border-radius:12px;
-  background:#fff;
-  border:1px solid rgba(0,0,0,.12);
-  color: var(--bb-primary);
-  font-weight:600;
-  font-size:15px;
-  cursor:pointer;
-  transition: background .15s ease, border-color .15s ease, box-shadow .15s ease;
-}
-.email-toggle-btn:hover{
-  background:#FFFEF8;
-  border-color: var(--bb-accent);
-  box-shadow: 0 6px 18px rgba(0,0,0,.06);
-}
-
-/* Email / OTP form animation */
-.email-form,
-.otp-form{
-  animation: slideIn .2s ease-out both;
+.primary-google img{ filter:brightness(0) invert(1); }
+.method-btn{
+  display:flex; align-items:center; justify-content:center; gap:10px;
+  width:100%; height:48px; border-radius:12px; background:#fff;
+  border:1px solid rgba(0,0,0,.12); color:var(--bb-primary);
+  font-weight:600; font-size:15px; cursor:pointer;
+  transition:background .15s,border-color .15s,box-shadow .15s;
 }
-@keyframes slideIn{
-  from{ opacity:0; transform: translateY(10px); }
-  to{ opacity:1; transform:none; }
+.method-btn:hover{
+  background:#FFFEF8; border-color:var(--bb-accent);
+  box-shadow:0 6px 18px rgba(0,0,0,.06);
 }
-
-/* Back button */
+.slide-in{ animation:slideIn .2s ease-out both; }
+@keyframes slideIn{ from{opacity:0;transform:translateY(10px)} to{opacity:1;transform:none} }
 .back-btn{
   display:inline-flex; align-items:center; justify-content:center;
-  width:100%; height:40px;
-  border-radius:10px;
-  background:transparent;
-  border:1px dashed rgba(0,0,0,.15);
-  color: var(--bb-primary);
-  font-weight:600;
-  font-size:14px;
-  cursor:pointer;
-  opacity:.8;
-  transition: opacity .15s ease, background .15s ease, border-color .15s ease;
-}
-.back-btn:hover{
-  opacity:1;
-  background:rgba(250,247,231,.9);
-  border-color: rgba(246,195,32,.6);
-}
-
-/* OTP field specific */
+  width:100%; height:40px; border-radius:10px; background:transparent;
+  border:1px dashed rgba(0,0,0,.15); color:var(--bb-primary);
+  font-weight:600; font-size:14px; cursor:pointer; opacity:.8;
+  transition:opacity .15s,background .15s,border-color .15s;
+}
+.back-btn:hover{ opacity:1; background:rgba(250,247,231,.9); border-color:rgba(246,195,32,.6); }
+.forgot-link{
+  background:transparent; border:none; padding:0; margin-top:4px;
+  align-self:flex-end; font-size:13px; font-weight:600;
+  color:var(--bb-primary); text-decoration:underline; cursor:pointer; opacity:.8;
+  transition:color .15s,opacity .15s;
+}
+.forgot-link:hover{ color:var(--bb-accent); opacity:1; }
 .otp-field{ justify-content:center; }
 .otp-input{
-  text-align:center;
-  font-size:28px !important;
-  letter-spacing:8px;
-  font-weight:700;
-  max-width: 200px;
+  text-align:center; font-size:28px !important;
+  letter-spacing:8px; font-weight:700; max-width:200px;
 }
 .resend-row{
-  display:flex;
-  align-items:center;
-  justify-content:center;
-  gap:8px;
-  margin-top:6px;
-}
-.resend-msg{
-  text-align:center;
-  font-size:13px;
-  color: var(--bb-primary);
-  opacity:.9;
-}
-/* Forgot password link */
-.forgot-link{
-  background: transparent;
-  border: none;
-  padding: 0;
-  margin-top: 4px;
-  align-self: flex-end;        /* right-align under the password field */
-  font-size: 13px;
-  font-weight: 600;
-  color: var(--bb-primary);
-  text-decoration: underline;
-  cursor: pointer;
-  opacity: .8;
-  transition: color .15s ease, opacity .15s ease;
-}
-.forgot-link:hover{
-  color: var(--bb-accent);
-  opacity: 1;
-}
-.forgot-link:focus-visible{
-  outline: 2px solid var(--bb-accent-2);
-  outline-offset: 2px;
-}
-
-
-/* Small screens */
-@media (max-width: 560px){
-  .auth-modal{ padding: 12px; }
-  .sheet{
-    width: min(96vw, 440px);
-    max-height: min(90vh, 680px);
-    border-radius: 18px;
-  }
-  .sheet-scroll{
-    border-radius: 0 0 18px 18px;
-  }
+  display:flex; align-items:center; justify-content:center; gap:8px; margin-top:6px;
+}
+.resend-msg{ text-align:center; font-size:13px; color:var(--bb-primary); opacity:.9; }
+@media (max-width:560px){
+  .auth-modal{ padding:12px; }
+  .sheet{ width:min(96vw,440px); max-height:min(90vh,700px); border-radius:18px; }
+  .sheet-scroll{ border-radius:0 0 18px 18px; }
 }
 `;
 
-
-
-const iosFontFix = `
-html { -webkit-text-size-adjust: 100%; }
-`;
+const iosFontFix = `html { -webkit-text-size-adjust: 100%; }`;
diff --git a/blossombuds-frontend/src/components/auth/RegisterModal.tsx b/blossombuds-frontend/src/components/auth/RegisterModal.tsx
index 0676e61..9e90c27 100644
--- a/blossombuds-frontend/src/components/auth/RegisterModal.tsx
+++ b/blossombuds-frontend/src/components/auth/RegisterModal.tsx
@@ -1,13 +1,13 @@
 import React, { useEffect, useRef, useState } from "react";
 import { createPortal } from "react-dom";
-import { customerRegister } from "../../api/auth";
+import { customerRegister, verifyPhoneOtp, resendPhoneOtp } from "../../api/auth";
 import { useNavigate, useLocation, Link } from "react-router-dom";
 import { useAuth } from "../../app/AuthProvider";
 import logo from "../../assets/BB_logo.svg";
 import { apiUrl } from "../../api/base";
 import http from "../../api/http";
 
-type Step = "choose" | "email-form" | "otp-verify";
+type Step = "choose" | "email-form" | "otp-verify" | "phone-form" | "phone-otp-verify";
 
 export default function RegisterModal() {
   const nav = useNavigate();
@@ -15,8 +15,9 @@ export default function RegisterModal() {
   const { loginWithToken } = useAuth();
 
   const [step, setStep] = useState<Step>("choose");
+
+  // Shared
   const [name, setName] = useState("");
-  const [email, setEmail] = useState("");
   const [password, setPassword] = useState("");
   const [confirm, setConfirm] = useState("");
   const [showPw, setShowPw] = useState(false);
@@ -24,16 +25,27 @@ export default function RegisterModal() {
   const [busy, setBusy] = useState(false);
   const [err, setErr] = useState<string | null>(null);
 
-  // OTP verification
-  const [otp, setOtp] = useState("");
-  const [otpBusy, setOtpBusy] = useState(false);
-  const [otpErr, setOtpErr] = useState<string | null>(null);
-  const [resendBusy, setResendBusy] = useState(false);
-  const [resendMsg, setResendMsg] = useState<string | null>(null);
+  // Email path
+  const [email, setEmail] = useState("");
+  const [emailOtp, setEmailOtp] = useState("");
+  const [emailOtpBusy, setEmailOtpBusy] = useState(false);
+  const [emailOtpErr, setEmailOtpErr] = useState<string | null>(null);
+  const [emailResendBusy, setEmailResendBusy] = useState(false);
+  const [emailResendMsg, setEmailResendMsg] = useState<string | null>(null);
+
+  // Phone path
+  const [phone, setPhone] = useState("");
+  const [whatsAppOptIn, setWhatsAppOptIn] = useState(false);
+  const [smsOptIn, setSmsOptIn] = useState(false);
+  const [phoneOtp, setPhoneOtp] = useState("");
+  const [phoneOtpBusy, setPhoneOtpBusy] = useState(false);
+  const [phoneOtpErr, setPhoneOtpErr] = useState<string | null>(null);
+  const [phoneResendBusy, setPhoneResendBusy] = useState(false);
+  const [phoneResendMsg, setPhoneResendMsg] = useState<string | null>(null);
 
   const sheetRef = useRef<HTMLDivElement>(null);
 
-  // Lock page scroll while modal is open (iOS-safe)
+  // Scroll lock (iOS-safe)
   useEffect(() => {
     const scrollY = window.scrollY;
     const prev = {
@@ -58,23 +70,29 @@ export default function RegisterModal() {
     };
   }, []);
 
-  // Validation
+  // Shared validations
   const nameOk = name.trim().length >= 2;
-  const emailOk = /^\S+@\S+\.\S+$/.test(email);
   const pwOk = password.length >= 8;
   const confirmOk = confirm === password && confirm.length > 0;
-  const canSubmit = nameOk && emailOk && pwOk && confirmOk;
 
-  // Step 1: Submit registration form
-  const onSubmit = async (e: React.FormEvent) => {
+  // Email path validations
+  const emailOk = /^\S+@\S+\.\S+$/.test(email);
+  const canEmailSubmit = nameOk && emailOk && pwOk && confirmOk;
+
+  // Phone path validations
+  const rawPhone = phone.replace(/\D/g, "");
+  const phoneOk = /^[6-9]\d{9}$/.test(rawPhone);
+  const canPhoneSubmit = nameOk && phoneOk && pwOk && confirmOk;
+
+  // ── Email path ─────────────────────────────────────────────────────────────
+
+  const submitEmailForm = async (e: React.FormEvent) => {
     e.preventDefault();
-    if (!canSubmit) return;
+    if (!canEmailSubmit) return;
     setBusy(true);
     setErr(null);
-
     try {
-      await customerRegister({ name, email, password, phone: undefined } as any);
-      // Move to OTP verification step
+      await customerRegister({ name, email, password });
       setStep("otp-verify");
     } catch (e: any) {
       setErr(e?.response?.data?.message || "Registration failed. Try a different email.");
@@ -83,47 +101,84 @@ export default function RegisterModal() {
     }
   };
 
-  // Step 2: Verify OTP and auto-login
-  const verifyOtp = async (e: React.FormEvent) => {
+  const verifyEmailOtp = async (e: React.FormEvent) => {
     e.preventDefault();
-    if (otp.length < 6) return;
-    setOtpBusy(true);
-    setOtpErr(null);
-
+    if (emailOtp.length < 6) return;
+    setEmailOtpBusy(true);
+    setEmailOtpErr(null);
     try {
-      // Verify the OTP - backend returns token on success
-      const { data } = await http.post("/api/customers/auth/verify-email-otp", { email, code: otp });
-
-      // Auto-login with returned token
-      if (data?.token) {
-        loginWithToken(data.token);
-        nav("/profile", { replace: true });
-      } else {
-        // Fallback: try login with email/password
-        const loginRes = await http.post("/api/customers/auth/login", { identifier: email, password });
-        if (loginRes.data?.token) {
-          loginWithToken(loginRes.data.token);
-          nav("/profile", { replace: true });
-        }
-      }
+      const { data } = await http.post("/api/customers/auth/verify-email-otp", { email, code: emailOtp });
+      if (data?.token) loginWithToken(data.token);
+      nav("/profile", { replace: true });
     } catch (e: any) {
-      setOtpErr(e?.response?.data?.message || "Invalid or expired code. Please try again.");
+      setEmailOtpErr(e?.response?.data?.message || "Invalid or expired code. Please try again.");
     } finally {
-      setOtpBusy(false);
+      setEmailOtpBusy(false);
     }
   };
 
-  // Resend OTP
-  const resendOtp = async () => {
-    setResendBusy(true);
-    setResendMsg(null);
+  const resendEmailOtp = async () => {
+    setEmailResendBusy(true);
+    setEmailResendMsg(null);
     try {
       await http.post("/api/customers/auth/resend-verification", { email });
-      setResendMsg("New code sent! Check your email.");
+      setEmailResendMsg("New code sent! Check your email.");
     } catch {
-      setResendMsg("Could not resend. Please try again.");
+      setEmailResendMsg("Could not resend. Please try again.");
     } finally {
-      setResendBusy(false);
+      setEmailResendBusy(false);
+    }
+  };
+
+  // ── Phone path ─────────────────────────────────────────────────────────────
+
+  const submitPhoneForm = async (e: React.FormEvent) => {
+    e.preventDefault();
+    if (!canPhoneSubmit) return;
+    setBusy(true);
+    setErr(null);
+    try {
+      await customerRegister({
+        name,
+        password,
+        phone: rawPhone,
+        whatsAppOptIn,
+        smsOptIn,
+      });
+      setStep("phone-otp-verify");
+    } catch (e: any) {
+      setErr(e?.response?.data?.message || "Registration failed. Try a different number.");
+    } finally {
+      setBusy(false);
+    }
+  };
+
+  const verifyPhoneOtpStep = async (e: React.FormEvent) => {
+    e.preventDefault();
+    if (phoneOtp.length < 6) return;
+    setPhoneOtpBusy(true);
+    setPhoneOtpErr(null);
+    try {
+      const result = await verifyPhoneOtp({ phone: rawPhone, code: phoneOtp });
+      if (result?.token) loginWithToken(result.token);
+      nav("/profile", { replace: true });
+    } catch (e: any) {
+      setPhoneOtpErr(e?.response?.data?.message || "Invalid or expired code. Please try again.");
+    } finally {
+      setPhoneOtpBusy(false);
+    }
+  };
+
+  const resendPhoneOtpStep = async () => {
+    setPhoneResendBusy(true);
+    setPhoneResendMsg(null);
+    try {
+      await resendPhoneOtp({ phone: rawPhone });
+      setPhoneResendMsg("New code sent to your phone!");
+    } catch {
+      setPhoneResendMsg("Could not resend. Please try again.");
+    } finally {
+      setPhoneResendBusy(false);
     }
   };
 
@@ -133,7 +188,7 @@ export default function RegisterModal() {
     <>
       <style>{styles}</style>
       <div className="auth-scrim" onClick={close} />
-      <div className="auth-modal" role="dialog" aria-modal="true" aria-label="Register form" onClick={close}>
+      <div className="auth-modal" role="dialog" aria-modal="true" aria-label="Register" onClick={close}>
         <div ref={sheetRef} className="sheet" onClick={(e) => e.stopPropagation()}>
           <header className="am-head">
             <div className="brand">
@@ -147,14 +202,14 @@ export default function RegisterModal() {
           </header>
 
           <div className="sheet-scroll">
-            {/* STEP: Choose method */}
+
+            {/* ── CHOOSE ──────────────────────────────────────────────── */}
             {step === "choose" && (
               <>
                 <div className="hero">
                   <h3>Create your account</h3>
                   <p>Join Blossom Buds Floral Artistry.</p>
                 </div>
-
                 <div className="form">
                   <button
                     type="button"
@@ -164,17 +219,13 @@ export default function RegisterModal() {
                     <img src="https://fonts.gstatic.com/s/i/productlogos/googleg/v6/24px.svg" alt="" />
                     <span>Sign up with Google</span>
                   </button>
-
                   <div className="divider"><span>or</span></div>
-
-                  <button
-                    type="button"
-                    className="email-toggle-btn"
-                    onClick={() => setStep("email-form")}
-                  >
-                    Sign up with Email
+                  <button type="button" className="method-btn" onClick={() => { setErr(null); setStep("email-form"); }}>
+                    <IconMail /> Sign up with Email
+                  </button>
+                  <button type="button" className="method-btn" onClick={() => { setErr(null); setStep("phone-form"); }}>
+                    <IconPhone /> Sign up with Mobile
                   </button>
-
                   <div className="links single">
                     <span className="muted">Already have an account?</span>
                     <Link
@@ -189,168 +240,214 @@ export default function RegisterModal() {
               </>
             )}

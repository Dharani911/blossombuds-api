<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Audit log of individual payment reminder sends (per channel: EMAIL, WHATSAPP, SMS). -->
    <changeSet id="0039-01-create-payment-reminders-table" author="dharani">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="payment_reminders"/>
            </not>
        </preConditions>
        <createTable tableName="payment_reminders">
            <column name="id" type="bigserial">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="checkout_intent_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="customer_id" type="bigint"/>
            <column name="channel" type="varchar(30)">
                <constraints nullable="false"/>
            </column>
            <column name="phone" type="varchar(30)"/>
            <column name="email" type="varchar(255)"/>
            <column name="status" type="varchar(40)" defaultValue="PENDING">
                <constraints nullable="false"/>
            </column>
            <column name="provider_message_id" type="varchar(200)"/>
            <column name="error_message" type="text"/>
            <column name="scheduled_at" type="timestamptz">
                <constraints nullable="false"/>
            </column>
            <column name="sent_at" type="timestamptz"/>
            <column name="failed_at" type="timestamptz"/>
            <column name="active" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="created_by" type="varchar(100)"/>
            <column name="created_at" type="timestamptz">
                <constraints nullable="false"/>
            </column>
            <column name="modified_by" type="varchar(100)"/>
            <column name="modified_at" type="timestamptz">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <rollback>
            <dropTable tableName="payment_reminders"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
